package ${package}.dao;

import java.util.List;
import ${package}.model.${entityName};

/**
* ${modelRemark}
* @author ${author}
* @project ${project}
* @date  ${now?datetime}
*/
public interface ${modelName?substring(1)}Mapper extends BaseMapper<${entityName}>{

}