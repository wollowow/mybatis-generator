package ${package}.service;

import ${package}.entity.${entityName};
import java.util.List;

/**
 * ${modelRemark} Service
 * @author ${author}
 * @project ${project}
 * @date  ${now?datetime}
 */
public interface ${modelName?substring(1)}Service extends IService<${entityName}>{


}